#include "monty.h"
/**
* op_swap - function that delete top value
* @stack: double linkes list that allocate data
* @line_number: line of file that have been read
*/
void op_swap(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}

